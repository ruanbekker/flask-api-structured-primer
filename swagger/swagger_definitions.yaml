swagger: "2.0"
info:
  title: Product API
  description: API for managing products
  version: "1.0"
definitions:
  Product:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
      description:
        type: string
      price:
        type: number
      inventory:
        type: integer